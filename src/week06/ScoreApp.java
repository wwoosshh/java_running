package week06;

import java.util.List;

/**
 * 프로그램 진입점.
 *
 * Week 5 LibraryMain과 의도적으로 다른 점:
 *  - Scanner 없음        (사용자 입력 없음)
 *  - 메뉴 루프 없음       (한 번 실행하고 종료)
 *  - try-catch 없음      (예외 처리 신경 X)
 *  - 데이터 하드코딩      (DB도 입력도 없이 List.of로 박아둠)
 *
 * 왜? Week 6의 목표는 "Stream/람다 문법에 익숙해지는 것" 하나입니다.
 *     UI/입력/예외에 뇌 자원을 쓰지 마세요.
 *
 * 백엔드 관점의 비유:
 *  - Student        ≈ Entity      (DB 한 행에 매핑될 데이터)
 *  - ScoreStats     ≈ Service     (비즈니스 로직 - 상태 없음)
 *  - ScoreApp.main  ≈ Controller  (Service를 호출해 결과를 응답으로 내보냄)
 *
 *  지금은 콘솔 println이 응답입니다. 나중에 Spring에서는 JSON이 됩니다.
 */
public class ScoreApp {

    public static void main(String[] args) {

        // ────────────────────────────────────────────
        // 1) 데이터 준비 (하드코딩)
        // ────────────────────────────────────────────
        List<Student> students = List.of(
                new Student("김지훈", 92),
                new Student("이서연", 78),
                new Student("박민재", 45),
                new Student("최유진", 88),
                new Student("정도윤", 67)
        );
        ScoreStats stats = new ScoreStats();

        System.out.println("===== 학생 성적 집계 =====");

        // ────────────────────────────────────────────
        // 2) 평균 점수 출력 (소수점 둘째자리까지)
        //    출력 예: "평균: 74.00"
        //    힌트: System.out.printf("평균: %.2f%n", stats.average(students));
        // ────────────────────────────────────────────
        // TODO

        // ────────────────────────────────────────────
        // 3) 전체 합계 출력
        //    출력 예: "총점: 370"
        // ────────────────────────────────────────────
        // TODO

        // ────────────────────────────────────────────
        // 4) 최고 점수 학생 출력
        //    출력 예: "최고: 김지훈(92점)"
        //    힌트: Student의 toString()이 잘 구현되어 있으면 그대로 println 가능
        // ────────────────────────────────────────────
        // TODO

        // ────────────────────────────────────────────
        // 5) 60점 이상 합격자 "이름" 리스트 출력
        //    출력 예: "합격자: [김지훈, 이서연, 최유진, 정도윤]"
        // ────────────────────────────────────────────
        // TODO

        // ────────────────────────────────────────────
        // 6) 합격자 수 / 전체 수 출력
        //    출력 예: "합격: 4명 / 전체: 5명"
        //    힌트: students.size()는 int, passCount는 long
        // ────────────────────────────────────────────
        // TODO

        // ────────────────────────────────────────────
        // 7) 점수 내림차순으로 전체 학생 출력 (한 줄에 한 명)
        //    힌트: stats.sortedDesc(students).forEach(System.out::println);
        // ────────────────────────────────────────────
        System.out.println("--- 점수 순위 ---");
        // TODO

        // ────────────────────────────────────────────
        //  도전 과제 (선택) — 막히면 패스해도 OK
        // ────────────────────────────────────────────
        // 8) 평균보다 높은 점수를 받은 학생들의 이름만 출력
        //    힌트: 평균을 먼저 변수에 담고 → students.stream().filter(...) 안에서 사용
        //    주의: 람다 안에서 쓰는 외부 변수는 final 또는 "사실상 final" 이어야 합니다
        //
        // 9) 점수를 10점 단위로 그룹핑해서 인원 수 세기
        //    예: 90점대 1명, 80점대 1명, 70점대 1명, 60점대 1명, 40점대 1명
        //    힌트: Collectors.groupingBy(s -> s.getScore() / 10, Collectors.counting())
        //         → Map<Integer, Long> 반환
    }
}
